# coolstep drop-in: desktop profile
# Generated by scripts/coolstep_init.sh — edit the template, not this file.
# Template: scripts/dropin-templates/desktop.conf
#
# CPUQuota: 30% — more headroom for faster KNN + backfill jobs
# MemoryMax: 1G — desktops often have larger workload rings
# Actuator: allowed (user must pass --no-armed to skip)
#
# Slice: omitted; defaults to user.slice.  Add a higher-numbered drop-in
# to pin a custom slice.
[Service]
CPUQuota=30%
MemoryMax=1G
